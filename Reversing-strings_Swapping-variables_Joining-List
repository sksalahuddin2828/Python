# Swapping variables more protected values 
a = 1
b = 2
a, b = b, a
print(f"a = {a}, b = {b}")

# Reversing strings

strings = 'ebircsbus'
print(strings[::-1])

# Joining List using join() functions

list = ["Leave", "A", "Like"]
join = " ".join(list)
print(join)
